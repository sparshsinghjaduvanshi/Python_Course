'''vertual environment is a tool used to isolate specific python environments on a single machine.
    Allowing you to work on multiple projects with dependencies and packages without conflicts.This can be especially useful when 
        working on projects that have conflicting package various or packages that are not compatible with each other.

    To create a virtual environment in python ,you can use the "venv" module that coes with puthon .Here's an example of 
        to create a virtual environment and active it:'''

# To create a virtual environment
# python -m venv myenv

# activate the virtual environment(linux/mac05)
# myenv/bin/activate

# activate the virtual environment(windows)
# myenv\Scripts\activate.bat

# deactivate the virtual environment(windows)
# deactivate
