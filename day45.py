import sparsh

sparsh.welcome()
'''the if__name__=="__main__" idiom is a common pattern used in python scripts to determine
 whether the script is being run directly or being imported as a module into script.'''

'''In python ,the __name__ varible is a built-in variable that is automatically set 
    to the name of the current module .When a python script is run directly the __name__ cariable is set to the string
    __main__ when the script is imported as a module into another script,
    the __name__ varible is set to the name of the module.'''